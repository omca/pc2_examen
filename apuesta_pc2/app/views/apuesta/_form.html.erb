<%= form_for(@apuestum) do |f| %>
  <% if @apuestum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apuestum.errors.count, "error") %> prohibited this apuestum from being saved:</h2>

      <ul>
      <% @apuestum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Tipo_Apuesta %><br />
    <%= f.collection_select :id, TipoApuestum.all, :id, :name, :prompt => true %>
  </div>

  <div class="field">
    <%= f.label :Monto_apuesta %><br />
    <%= f.number_field :amount_apuesta %>
  </div>
  <div class="field">
    <%= f.label :Monto_pagado %><br />
    <%= f.number_field :amount_pagado %>
  </div>

  <div class="field">
    <%= f.label :Caballos %><br />
    <%= f.collection_select :id, Caballo.all, :id, :name, :prompt => true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
