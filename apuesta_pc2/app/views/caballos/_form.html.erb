<%= form_for(@caballo) do |f| %>
  <% if @caballo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caballo.errors.count, "error") %> prohibited this caballo from being saved:</h2>

      <ul>
      <% @caballo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ratio %><br />
    <%= f.number_field :ratio %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
