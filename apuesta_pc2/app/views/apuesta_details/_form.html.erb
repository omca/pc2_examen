<%= form_for(@apuesta_detail) do |f| %>
  <% if @apuesta_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apuesta_detail.errors.count, "error") %> prohibited this apuesta_detail from being saved:</h2>

      <ul>
      <% @apuesta_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
